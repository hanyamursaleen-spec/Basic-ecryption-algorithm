# Basic-ecryption-algorithm
This project implements a basic Caesar Cipher using a fixed shift of 13 positions (commonly known as ROT13). Each letter in the input string is shifted 13 places forward in the English alphabet. If the shift goes past Z, it wraps around to the beginning of the alphabet. 
